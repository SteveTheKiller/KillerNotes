<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- KillerNotes half of the 98SE theme. Shared contract tokens live in
         KillerUI/Themes/98SE.xaml; only app-specific resources belong here. Flattened
         from the former Light-plus-patch form so this theme stands alone.
         Accent variants still live in Themes/Accents/98SE/. -->
    <sys:Double x:Key="GrainOpacity">0</sys:Double>
    <sys:Double x:Key="IconShadowOpacity">0</sys:Double>

    <!-- 98SE is deliberately FLAT: no drop shadows, a hard 2px frame, and raised-control
         bevels instead. These override the material defaults LoadDict applies. -->
    <sys:Double x:Key="PaneShadowOpacity">0</sys:Double>
    <sys:Double x:Key="BarShadowOpacity">0</sys:Double>
    <sys:Double x:Key="FlyoutShadowOpacity">0</sys:Double>
    <Thickness x:Key="WindowFrameThickness">2</Thickness>
    <Thickness x:Key="BevelLightThickness">2,2,0,0</Thickness>
    <Thickness x:Key="BevelDarkThickness">0,0,2,2</Thickness>
    <SolidColorBrush x:Key="BevelLightBrush" Color="#ffffff" po:Freeze="True"/>
    <SolidColorBrush x:Key="BevelDarkBrush"  Color="#808080" po:Freeze="True"/>

    <!-- The About card's info panel. Every other theme inherits the context-menu fill from
         ThemeManager; here that would be the same #c0c0c0 button face as the card behind it,
         so the panel would vanish. White plus the crossed (sunken) bevel in MainWindow.xaml
         makes it read as a recessed Win98 client area, matching PaneBrush. -->
    <SolidColorBrush x:Key="AboutPanelBrush" Color="#ffffff" po:Freeze="True"/>

    <!-- Caption buttons. A real button face inset from the caption, so the title bar gradient
         ends AT the button rather than running under it - the thing that made the close glyph
         look painted onto the gradient. The inset turns the 44x36 caption slot into a ~20x16
         button, which is the Win98 proportion. The raised bevel comes from the Bevel* keys
         above and reverses on press, exactly like every other button in this theme.
         Close is a black glyph on the same face, not a red one: Win98 had no red close. -->
    <SolidColorBrush x:Key="CaptionButtonBrush"      Color="#c0c0c0" po:Freeze="True"/>
    <SolidColorBrush x:Key="CaptionCloseBrush"       Color="#000000" po:Freeze="True"/>
    <SolidColorBrush x:Key="CaptionCloseHoverBrush"  Color="#00000000" po:Freeze="True"/>
    <SolidColorBrush x:Key="CaptionCloseHoverFgBrush" Color="#000000" po:Freeze="True"/>
    <!-- Black glyphs, not ChromeTextBrush's white: the buttons have their own grey face now,
         so a white glyph would be invisible on it. -->
    <SolidColorBrush x:Key="CaptionGlyphBrush"       Color="#000000" po:Freeze="True"/>

    <!-- Win98 caption geometry: an 18px bar with buttons that fill it, 2px in from the frame
         and 1px apart. The wordmark shrinks to fit the shorter bar rather than being clipped. -->
    <sys:Double x:Key="TitleBarHeight">22</sys:Double>
    <sys:Double x:Key="CaptionButtonWidth">18</sys:Double>
    <sys:Double x:Key="CaptionButtonHeight">16</sys:Double>
    <Thickness x:Key="CaptionButtonMargin">0,0,1,0</Thickness>
    <sys:Double x:Key="TitleWordmarkSize">11</sys:Double>
    <sys:Double x:Key="TitleWordmarkBoldSize">14</sys:Double>
    <!-- The blurred shadow is off here (IconShadowOpacity 0 - the theme is flat), so the
         wordmark gets the hard 1px offset copy instead: a stroke, not a glow. -->
    <sys:Double x:Key="WordmarkEmbossOpacity">1</sys:Double>
    <!-- Tahoma, NOT "MS Sans Serif": MS Sans Serif is a raster .FON, and WPF cannot render bitmap
         fonts - it silently falls back to a default serif, which is why the captions came out in
         the wrong face. Tahoma is TrueType, shipped with Windows, and is the Win98-era UI font. -->
    <FontFamily x:Key="ChromeFontFamily">Tahoma</FontFamily>
    <Thickness x:Key="CaptionCloseGap">2,0,0,0</Thickness>
    <!-- The 2 at the top is the WindowFrameThickness, which MainWindow paints as a RowSpan=3
         overlay at ZIndex 9000 covering the top 2px of the caption. Insetting by it centres the
         buttons in the VISIBLE band (was 1px above / 3px below). 98SE only - the frame is 0
         everywhere else, so this key is 0 there and nothing moves. -->
    <Thickness x:Key="CaptionButtonsMargin">0,2,3,0</Thickness>
    <sys:Double x:Key="TitleIconSize">16</sys:Double>
    <Thickness x:Key="TitleIconMargin">0,0,4,0</Thickness>
    <!-- Not the family's 14px inset, but not flush either: 4 to MATCH the gap on the right, where
         the close button's face ends up 4px from the frame (CaptionButtonsMargin 3 +
         CaptionButtonMargin 1). At 2 the icon sat tighter to the left than the buttons did right.
         The 2 at the top is the frame inset, same reason as CaptionButtonsMargin above. -->
    <Thickness x:Key="TitleBarPadding">4,2,0,0</Thickness>

    <!-- The editor pane's inset. The family default is a flush TOP, and that is right on the other
         twelve because they BLEND the title bar into the surrounding chrome - the pane meeting it
         reads as one continuous surface. This theme's caption is a distinct coloured band, so the
         pane butts into a hard edge. 8 on top to match the 8 on the right. -->
    <Thickness x:Key="ContentPaneMargin">0,8,8,0</Thickness>

    <!-- Win98 client areas are WHITE, and both of these are client areas: an edit field and a list.
         The family rule that inputs take the darkest tone is right where the darkest tone is dark,
         but here it IS the button face, so the search box came out the same grey as its panel. -->
    <SolidColorBrush x:Key="SearchFieldBrush" Color="#ffffff" po:Freeze="True"/>
    <SolidColorBrush x:Key="ListPaneBrush"    Color="#ffffff" po:Freeze="True"/>
    <!-- No soft scroll fades - Win98 lists end at a hard edge. -->
    <sys:Double x:Key="EdgeFadeOpacity">0</sys:Double>

    <!-- No flat outline: in 98SE a button's edge IS the bevel (white top/left, grey bottom/right),
         drawn by the bevel borders in the OutlineButton template. A visible OutlineRestBrush here
         would draw a second, flat line in the same place. Transparent rather than absent, so the
         button still reserves its 1px border and the text does not shift.
         Window edge: transparent for the same reason - see WindowEdgeBrush below. -->
    <SolidColorBrush x:Key="OutlineRestBrush"      Color="#00000000" po:Freeze="True"/>

    <!-- The 1px line ringing the whole window. On the top and left it hides against the white
         bevel, but on the bottom and right it lands OUTSIDE the dark bevel as a bright stripe.
         Transparent keeps the 1px geometry, and the resize grab area, exactly as they were. -->
    <SolidColorBrush x:Key="WindowEdgeBrush"       Color="#00000000" po:Freeze="True"/>
    <!-- 0, not just a transparent brush: RootBorder wraps the whole window, so its border band
         sits outside the bevel. Left at 1px the window's own grey Background shows through it and
         you still get a light line outside the dark bottom/right bevel. -->
    <Thickness x:Key="WindowEdgeThickness">0</Thickness>

    <!-- A button's edge here is its bevel, so the flat outline must not also draw - two lines in
         the same place is what made the secondary buttons look doubled. -->
    <SolidColorBrush x:Key="ButtonEdgeBrush"       Color="#00000000" po:Freeze="True"/>

    <!-- Dialogs get a real caption band here, unlike the rest of the family where the title blends
         into the card. ThemeManager sees this marker and points DialogTitleBarBrush at whatever
         TitleBarBrush the current accent resolved to, so the band follows the accent gradient. -->
    <sys:Boolean x:Key="UseDialogCaption">True</sys:Boolean>
    <!-- 20, not 28: a Windows 98 caption is short. -->
    <sys:Double x:Key="DialogTitleBarHeight">20</sys:Double>
    <!-- No halo. There is no drop shadow to make room for, and the halo is the resize grab band -
         an invisible one puts the grab handle out in space beyond the visible window edge. -->
    <Thickness x:Key="DialogHaloMargin">0</Thickness>

    <SolidColorBrush x:Key="TextFooter"            Color="#000000" po:Freeze="True"/>
    <SolidColorBrush x:Key="RowSelectedBrush"      Color="#004f00" po:Freeze="True"/>
    <SolidColorBrush x:Key="MenuTextBrush"         Color="#000000" po:Freeze="True"/>
    <SolidColorBrush x:Key="OutlinePressedBrush"   Color="#a0a0a0" po:Freeze="True"/>
    <SolidColorBrush x:Key="ScrollThumbBrush"      Color="#c0c0c0" po:Freeze="True"/>
    <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#a0a0a0" po:Freeze="True"/>

    <SolidColorBrush x:Key="BgFlyout"              Color="#c0c0c0" po:Freeze="True"/>
    <SolidColorBrush x:Key="BgRecentPanel"         Color="#c0c0c0" po:Freeze="True"/>
    <SolidColorBrush x:Key="BgCanvas"              Color="#ffffff" po:Freeze="True"/>
    <!-- AccentLogo is deliberately NOT stated here. ThemeManager defaults it to HeaderLineBrush
         after the accent overlay merges, and all six 98SE accents restate HeaderLineBrush, so the
         "Notes" run follows whichever accent is chosen instead of being pinned. It used to be a
         hardcoded #ffff00, which is why it came out yellow whatever the accent was. -->

    <SolidColorBrush x:Key="SelectionAccent"       Color="#004f00" po:Freeze="True"/>
    <SolidColorBrush x:Key="RadioAccent"           Color="#004f00" po:Freeze="True"/>
    <SolidColorBrush x:Key="SliderTrack"           Color="#808080" po:Freeze="True"/>
    <SolidColorBrush x:Key="SettingsOpenRowBg"     Color="#a0a0a0" po:Freeze="True"/>
    <!-- Button FACE, not white. The sort trio are buttons sitting on the window face, not client
         areas, so they take the same grey as every other button and only light up on hover.
         White at rest read as inverted next to the search field beside them. -->
    <SolidColorBrush x:Key="SortButtonBrush"       Color="#c0c0c0" po:Freeze="True"/>

    <!-- Shortcut-overlay category colours (the family neon set, shared with the website map) -->
    <SolidColorBrush x:Key="KnCatFile"   Color="#00fff9" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatNote"   Color="#ff2975" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatFormat" Color="#ffd319" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatView"   Color="#f222ff" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatSearch" Color="#00b3ff" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatHelp"   Color="#39ff14" po:Freeze="True"/>
    <SolidColorBrush x:Key="KnCatEdit"   Color="#ff901f" po:Freeze="True"/>
</ResourceDictionary>
